apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: tinykv-route
  namespace: tinykv
spec:
  parentRefs:
  - name: tinykv-gateway
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: tinykv-service
      port: 8080
